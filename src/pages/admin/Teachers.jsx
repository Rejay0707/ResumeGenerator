import React from "react";
import { Typography } from "@mui/material";

export default function Teachers() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Manage Teachers
      </Typography>
      <Typography>List, add, edit, and delete teachers here.</Typography>
    </div>
  );
}